def iou(detection, ground_truths):
    """
    compute intersection over union for every ground_truth object in ground_truths list


    :param detection:
    :param ground_truths: list
    :return: list
    """

    # TODO
    return [0 for _ in range(len(ground_truths))]
